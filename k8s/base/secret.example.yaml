# TEMPLATE ONLY — do NOT commit real secrets.
#
# In production, generate this Secret out-of-band. Recommended approaches:
#   * Sealed Secrets (bitnami) — commit an encrypted SealedSecret instead.
#   * External Secrets Operator — sync from AWS Secrets Manager / Vault / GCP SM.
#   * `kubectl create secret generic ... --from-literal=...` in your pipeline.
#
# Create the real Secret with the same name/keys, e.g.:
#   kubectl -n photoplatform create secret generic platform-secrets \
#     --from-literal=DJANGO_SECRET_KEY=$(openssl rand -hex 32) \
#     --from-literal=POSTGRES_USER=photo \
#     --from-literal=POSTGRES_PASSWORD=... \
#     --from-literal=MINIO_ROOT_USER=... \
#     --from-literal=MINIO_ROOT_PASSWORD=... \
#     --from-literal=DJANGO_SUPERUSER_EMAIL=admin@example.com \
#     --from-literal=DJANGO_SUPERUSER_PASSWORD=...
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: photoplatform
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "REPLACE_ME_WITH_A_LONG_RANDOM_VALUE"
  POSTGRES_USER: "photo"
  POSTGRES_PASSWORD: "REPLACE_ME"
  MINIO_ROOT_USER: "REPLACE_ME"
  MINIO_ROOT_PASSWORD: "REPLACE_ME"
  DJANGO_SUPERUSER_EMAIL: "admin@example.com"
  DJANGO_SUPERUSER_PASSWORD: "REPLACE_ME"
